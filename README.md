# stock-prediction-with-deep-learning-analysis

updating...

Package needed:
```shell script
pip install dash
pip install yfinance
pip install finta
pip install nltk
pip install wordcloud
pip install tweet-preprocessor
pip install langdetect
pip install plotly
```